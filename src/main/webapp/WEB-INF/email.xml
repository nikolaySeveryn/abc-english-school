<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.5.xsd
						http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-2.5.xsd
						http://www.springframework.org/schema/util  http://www.springframework.org/schema/util/spring-util.xsd">
	
	<!-- <util:properties id="hibernateProperties" location="classpath:config/email.properties"/> -->
	<bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
    	<property name="locations" value="classpath:config/email.properties"/>
	</bean>
	
	<bean id="mailSender" class="org.springframework.mail.javamail.JavaMailSenderImpl">
	  	<property name="host" value="${email.server.host}"/>
	  	<property name="port" value="${email.server.port}"/>
	  	<property name="username" value="${email.from.username}"/>
	 	<property name="password" value="${email.from.password}"/>
	  	<property name="javaMailProperties">
			<props>
			    <prop key="mail.transport.protocol">${email.protocol}</prop>
			    <prop key="mail.smtp.auth">${email.smtpAuth}</prop>
			    <prop key="mail.smtp.starttls.enable">${email.starttlsEnable}</prop>
			    <prop key="mail.debug">${email.debug}</prop>
			</props>
  		</property>
    </bean>
</beans>

 
